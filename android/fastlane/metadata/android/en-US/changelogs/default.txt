• A linked device no longer holds a copy of your master key — each runs on its own keys, so removing a lost or stolen device can now cut it off cryptographically. Turns on per circle as everyone updates.
• Storage cleanup: see downloads by size, delete to free space, keep a re-downloadable placeholder, pin favourites, set age/size limits that really delete.
• Video is compressed on posting; story styling now matches across platforms; closed a video location-metadata leak.
